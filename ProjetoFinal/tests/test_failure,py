from client.client_core import ClientCore


def test_invalid_movie_id():
    core = ClientCore()
    core.connect()

    result = core.list_screenings_for_movie("abc")

    assert result["status"] == "error"

    core.close()